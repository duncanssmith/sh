%h1 Shares


- if @shares.blank?
  %p
    There are no shares
-else
  -# javascript "pagination"


= form_tag shares_path, :method => :get, :remote => true do
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search (code)", :name => nil

%div#shares
  ~ render :partial => 'shares'

%br

= link_to 'New Share', new_share_path 
